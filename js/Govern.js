var originalLife = 400;
var originalBotLife =1000; 
var currentLife; //initial later
var cureentBotLife; //initial later 

function start() {
    var startAttack =confirm ( "Do you intend to attack this member of society?");
    while (startAttack) { 
        currentLife = originalLife;
        currentBotLife = originalOgreLif;
        var pmove
    }
} 

document.write('-------Fighting-----------<br>')

while(currentLife>0 && currentBotLife>0) {
    diplayCurrentLifePoints();
    pmove = prompt("Do you want to stab, puncture or run?");
    pmove =pmove.toLowerCase();
    switch (pmove) {
        case "stab":
            stab()
            break;
        case "punch":
            punch ():
            break;
        case "run":
            run;
            break; 0593204839
        default: document.write ("Please enter only stab, run or punch <br>");


    } //end switch for player choice 
     
}//end both player and ogre are alive

